// tests for <%= name %> endpoints. If using `fetch`, we recommend using
// `fetch-mock` to mock calls/responses. Out of the box, we're using
// `chai`s set of `expect` APIs, which look like:
//
//      describe('store/<%= name %>/endpoints', function () {
//        describe('#fetchSomething', function () {
//          it('makes an API call', function () {
//            fetchMock.get('/api/catalog.json', '{ "status": "ok" }'')
//            endpoints.fetchSomething().then(() => {
//              expect(fetchMock.calls().matched).to.have.lengthOf(1)
//            })
//          })
//        })
//      })

import { expect } from 'chai'
import * as endpoints from './endpoints'

describe('store/<%= name %>/endpoints', function () {

})
