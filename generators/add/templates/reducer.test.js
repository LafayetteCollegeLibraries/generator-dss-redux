// Tests for the reducer. Out of the box, we're using `chai`s set
// of `expect` APIs, though this can be modified. Tests look like:
//
//     describe('store/<%= name %>/reducer', function () {
//       describe('actions#doSomething', function () {
//         it('works!', function () {
//           const result = reducer(initialState, actions.doSomething())
//           expect(result).to.not.be.null
//         })
//       })
//     })

import { expect } from 'chai'
import * as actions from './actions'
import reducer, { initialState } from './reducer'

describe('store/<%= name %>/reducer', function () {

})
